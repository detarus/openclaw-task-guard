#!/usr/bin/env bash
set -euo pipefail

if [ "${NO_BUMP:-0}" = "1" ]; then
  exit 0
fi

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

node scripts/bump-version.mjs >/dev/null

git add VERSION
